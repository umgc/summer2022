import 'package:test/test.dart';
import '../lib/barcode_scanner.dart';

void main() {
  test('Scanner must return QR Code link', () {
    final barcodeScanner = BarcodeScannerApi();

    //expect(barcode.value, 1);
  });
}
